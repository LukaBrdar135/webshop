import classes from "./FooterAnimation.module.css";

const FooterAnimation = () => {
  return (
    <div className={classes.animationHolder}>
      <div className={classes.animated}></div>
    </div>
  );
};

export default FooterAnimation;
